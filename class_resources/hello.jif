fun max(a,b)
{
	if (a > b) {
	return a;
	}
	else {
		return b;
	}
}

fun main()
{
	write("enter first number");
	read(num1);

	write("enter second number");
	read(num2);

	big = max(num1,num2);
	write("bigger number is: ");
	write(big);

}

